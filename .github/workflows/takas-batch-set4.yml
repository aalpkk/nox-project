name: Takas Batch SET4 (ISGYO AKSA LIDER SEKUR ESCOM CEOEM GSDHO)
on:
  workflow_dispatch:

jobs:
  takas-batch-set4:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests
      - name: Run takas batch set4
        env:
          MATRIKS_API_KEY: ${{ secrets.MATRIKS_API_KEY }}
          MATRIKS_CLIENT_ID: ${{ secrets.MATRIKS_CLIENT_ID }}
          PYTHONPATH: .
        run: python tools/takas_batch_2026_05_15_set4.py
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: takas-batch-2026-05-15-set4
          path: |
            output/takas_batch_2026-05-15_set4.csv
            output/takas_batch_2026-05-15_set4_ice.csv
            output/takas_batch_2026-05-15_set4.md
          if-no-files-found: warn
